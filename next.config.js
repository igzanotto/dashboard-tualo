/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com', 'uualvelmahgeavaoxviu.supabase.co', 'vizzlo.com', 'visualize.graphy.app'],
      },
};

module.exports = nextConfig;
